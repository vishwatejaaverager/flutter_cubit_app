import 'package:flutter/material.dart';

class BarItemePage extends StatelessWidget {
  const BarItemePage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          "bar Page"
        ),
      ),
    );
  }
}